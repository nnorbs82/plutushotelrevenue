
(function(g){'use strict';const n=v=>v===''||v==null?NaN:Number(v);function calculate(x){const v={rooms:n(x.rooms),days:n(x.days),adr:n(x.adr),variableCost:n(x.variableCost),fixedCost:n(x.fixedCost),currentOccupancy:n(x.currentOccupancy),targetProfit:n(x.targetProfit),ancillaryRevenue:n(x.ancillaryRevenue),otaShare:n(x.otaShare),otaCommission:n(x.otaCommission),paymentFee:n(x.paymentFee)};for(const k of ['currentOccupancy','targetProfit','ancillaryRevenue','otaShare','otaCommission','paymentFee'])if(!Number.isFinite(v[k]))v[k]=0;const errors=[];if(!Number.isFinite(v.rooms)||v.rooms<=0)errors.push('Enter rooms.');if(!Number.isFinite(v.days)||v.days<=0)errors.push('Enter days.');if(!Number.isFinite(v.adr)||v.adr<0)errors.push('Enter ADR.');if(!Number.isFinite(v.variableCost)||v.variableCost<0)errors.push('Enter variable cost.');if(!Number.isFinite(v.fixedCost)||v.fixedCost<0)errors.push('Enter fixed costs.');if(errors.length)return{valid:false,errors};const available=v.rooms*v.days,rev=v.adr+v.ancillaryRevenue,ota=v.adr*(v.otaShare/100)*(v.otaCommission/100),fees=rev*(v.paymentFee/100),variable=v.variableCost+ota+fees,contribution=rev-variable;if(contribution<=0)return{valid:false,errors:['Revenue per occupied room must exceed variable costs.']};const beNights=v.fixedCost/contribution,beOcc=beNights/available*100,targetNights=(v.fixedCost+v.targetProfit)/contribution,targetOcc=targetNights/available*100,currentNights=v.currentOccupancy>0?available*v.currentOccupancy/100:null,currentProfit=currentNights==null?null:currentNights*contribution-v.fixedCost;const graph=[];for(let occ=0;occ<=100;occ+=5){const nights=available*occ/100;graph.push({occ,revenue:nights*rev,cost:v.fixedCost+nights*variable})}return{valid:true,values:v,available,rev,variable,contribution,beNights,beOcc,targetOcc,currentProfit,graph}}const api={calculate};if(typeof module!=='undefined'&&module.exports)module.exports=api;g.PlutusV9Calc=api})(typeof window!=='undefined'?window:globalThis);
